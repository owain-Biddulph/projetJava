package core;

import java.util.Comparator;

import person.Patient;

public interface PatientComparator extends Comparator<Patient>{
	//here we do a strategy pattern on how to sort the patients
}
